package com.memora.shop.entity;

/**
 * Raridade compartilhada por cosméticos e cenários — usada para
 * definir a faixa de preço sugerida e o destaque visual (cor do
 * badge) no catálogo da loja.
 */
public enum Raridade {
    COMUM,
    RARA,
    EPICA,
    LEGENDARIA
}
